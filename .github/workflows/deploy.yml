name: Setup JupyterHub on AWS
on:
  push:
   branches: [ main ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Setup and deploy
    runs-on: ubuntu-latest
    environment:
      name: AWS_ARN
    steps:
    - name: configure aws credentials
      uses: aws-actions/configure-aws-credentials@master
      with:
        role-to-assume: ${{ secrets.AWS_ARN }}
        role-duration-seconds: 900 # the ttl of the session, in seconds.
        aws-region: us-east-1 # use your region here.
    - name: test AWS
      run: |
        aws ec2 run-instances --image-id ami-00874d747dde814fa \
        --instance-type t2.xlarge \
        --key-name jhub-test-econ-ark \
        --count 1 \
        --user-data IyEvYmluL2Jhc2gKY3VybCAtTCBodHRwczovL3RsamguanVweXRlci5vcmcvYm9vdHN0cmFwLnB5IFwKICB8IHN1ZG8gcHl0aG9uMyAtIFwKICAgIC0tYWRtaW4gamh1Yi1lY29uLWFkbWluCg== \
        --tag-specifications name=gh-jhub
